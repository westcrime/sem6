class BooleanType {
    defaultValue;
    notNull;
    constructor(defaultValue, notNull) {
        this.defaultValue = defaultValue;
        this.type = 'BooleanType';
        this.notNull = notNull;
    }
}
export default BooleanType;
